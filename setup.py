from setuptools import setup, find_packages

setup(
    name='spn',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],  # Leave empty to avoid installing anything
    python_requires='>=3.6',
    description='Sum-Product Networks library',
)